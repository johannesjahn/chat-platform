apiVersion: v2
name: chat-platform
description: >-
  Helm chart for deploying the chat-platform backend, Postgres, and Redis to a
  Kubernetes cluster. The frontend (web/) is deployed separately to Cloudflare
  Workers — see ../README.md.
type: application
version: 0.16.3
appVersion: "0.16.3"
